(() => {
  'use strict';
  const STORAGE_KEY='morningMemory_v1';
  const PREF_KEY='morningMemory_prefs_v1';

  const seedWords=[
    ['Focus','Fokus','Mindset','Direct your attention toward what matters.'],['Courage','Keberanian','Character','Act despite fear or uncertainty.'],['Discipline','Disiplin','Habit','Do what matters consistently.'],['Consistency','Konsistensi','Habit','Repeat useful action over time.'],['Resilience','Ketangguhan','Character','Recover and continue after difficulty.'],['Persistence','Kegigihan','Character','Continue despite obstacles.'],['Purpose','Tujuan','Mindset','A meaningful reason behind action.'],['Growth','Pertumbuhan','Mindset','Progress through learning and adaptation.'],['Patience','Kesabaran','Character','Stay steady while waiting or working.'],['Action','Tindakan','Momentum','Turn intention into movement.'],['Progress','Kemajuan','Momentum','Movement toward a desired result.'],['Commitment','Komitmen','Character','Remain dedicated to a chosen course.'],['Confidence','Kepercayaan diri','Mindset','Trust your ability to act and learn.'],['Determination','Tekad','Character','Firm intention to achieve something.'],['Clarity','Kejelasan','Mindset','Know what matters and what comes next.'],['Energy','Energi','Wellbeing','Capacity to engage and act.'],['Balance','Keseimbangan','Wellbeing','Manage competing demands sustainably.'],['Gratitude','Rasa syukur','Wellbeing','Notice and appreciate what is valuable.'],['Curiosity','Keingintahuan','Learning','Desire to explore and understand.'],['Learning','Pembelajaran','Learning','Acquire knowledge or skill through experience.'],['Practice','Latihan','Learning','Repeated effort to improve performance.'],['Adaptability','Kemampuan beradaptasi','Character','Adjust effectively to changing conditions.'],['Initiative','Inisiatif','Momentum','Start useful action without waiting.'],['Responsibility','Tanggung jawab','Character','Own your duties and consequences.'],['Integrity','Integritas','Character','Act consistently with sound principles.'],['Kindness','Kebaikan','Character','Act with care toward others.'],['Empathy','Empati','Character','Understand another person’s perspective.'],['Optimism','Optimisme','Mindset','Expect workable possibilities while staying realistic.'],['Hope','Harapan','Mindset','Believe a desired future remains possible.'],['Dream','Impian','Mindset','A desired future worth pursuing.'],['Vision','Visi','Mindset','A clear picture of a desired future.'],['Momentum','Momentum','Momentum','Progress that makes continued action easier.'],['Effort','Upaya','Momentum','Energy applied toward a task.'],['Strength','Kekuatan','Character','Capacity to withstand or act effectively.'],['Bravery','Keberanian','Character','Willingness to face danger or difficulty.'],['Calm','Ketenangan','Wellbeing','A steady, less-reactive state.'],['Mindfulness','Kesadaran penuh','Wellbeing','Attend deliberately to the present moment.'],['Reflection','Refleksi','Learning','Think carefully about experience and meaning.'],['Creativity','Kreativitas','Learning','Produce useful or original ideas.'],['Excellence','Keunggulan','Mindset','Pursue a high standard of performance.'],['Ambition','Ambisi','Mindset','Strong desire to achieve something significant.'],['Achievement','Pencapaian','Momentum','A result reached through effort.'],['Success','Kesuksesan','Momentum','Achievement of an intended outcome.'],['Challenge','Tantangan','Learning','A demanding task that tests capability.'],['Opportunity','Peluang','Momentum','A favorable opening for action or progress.'],['Preparation','Persiapan','Habit','Get ready before action is required.'],['Priority','Prioritas','Mindset','What deserves attention before alternatives.'],['Routine','Rutinitas','Habit','A repeated pattern of behavior.'],['Habit','Kebiasaan','Habit','A behavior repeated until it becomes easier to perform.'],['Attention','Perhatian','Mindset','Selective focus on relevant information.'],['Awareness','Kesadaran','Mindset','Notice internal or external conditions.'],['Choice','Pilihan','Mindset','A selected option among alternatives.'],['Decision','Keputusan','Mindset','A resolved choice about what to do.'],['Direction','Arah','Mindset','The course toward a target.'],['Goal','Sasaran','Mindset','A specific desired result.'],['Plan','Rencana','Momentum','An organized approach to future action.'],['Start','Mulai','Momentum','Begin an action or process.'],['Finish','Tuntas','Momentum','Bring an intended task to completion.'],['Improve','Meningkatkan','Learning','Make something better than before.'],['Explore','Menjelajah','Learning','Investigate beyond what is already known.'],['Create','Menciptakan','Learning','Bring a new idea or product into existence.'],['Build','Membangun','Momentum','Develop something step by step.'],['Lead','Memimpin','Character','Guide action toward a shared direction.'],['Support','Dukungan','Character','Help another person or process succeed.'],['Trust','Kepercayaan','Character','Rely on the integrity or ability of someone or something.'],['Respect','Hormat','Character','Recognize value and boundaries in others.'],['Humility','Kerendahan hati','Character','Maintain an accurate view of one’s importance and limits.'],['Wisdom','Kebijaksanaan','Learning','Use knowledge and judgment well.'],['Knowledge','Pengetahuan','Learning','Information and understanding acquired through learning.'],['Skill','Keterampilan','Learning','Learned ability to perform a task effectively.'],['Mastery','Penguasaan','Learning','High control or understanding of a skill or subject.'],['Recovery','Pemulihan','Wellbeing','Restore capacity after strain or difficulty.'],['Rest','Istirahat','Wellbeing','Pause activity to restore capacity.'],['Health','Kesehatan','Wellbeing','Physical and mental functioning and wellbeing.'],['Joy','Kegembiraan','Wellbeing','A feeling of pleasure or delight.'],['Peace','Kedamaian','Wellbeing','A state of calm and low conflict.'],['Connection','Keterhubungan','Wellbeing','A meaningful link with people or ideas.'],['Contribution','Kontribusi','Purpose','Add useful value to a larger effort.'],['Meaning','Makna','Purpose','Significance or value attached to an experience or goal.'],['Service','Pelayanan','Purpose','Useful action performed for others.'],['Impact','Dampak','Purpose','A meaningful effect produced by an action.']
  ];

  const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
  const today=()=>new Date().toISOString().slice(0,10);
  const uid=()=>Math.random().toString(36).slice(2,8)+Date.now().toString(36).slice(-5);
  const clamp=(n,a,b)=>Math.max(a,Math.min(b,n));
  const shuffle=a=>{const x=[...a];for(let i=x.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[x[i],x[j]]=[x[j],x[i]]}return x};
  const sample=(arr,n)=>shuffle(arr).slice(0,Math.min(n,arr.length));
  const normalize=s=>String(s||'').trim().toLocaleLowerCase('id-ID').replace(/[’']/g,"'").replace(/\s+/g,' ');
  const esc=s=>String(s??'').replace(/[&<>'"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[m]));

  let state=loadState();
  let prefs=loadPrefs();
  let currentGame=null;
  let audioCtx=null, musicNodes=[];

  function defaultState(){
    const now=Date.now();
    return {cards:seedWords.map((w,i)=>({id:uid(),english:w[0],indo:w[1],category:w[2],note:w[3],correct:0,wrong:0,streak:0,mastery:0,lastPlayed:null,nextReview:today(),createdAt:now+i})),stats:{correct:0,wrong:0,sessions:0,streak:0,lastSession:null,history:[]},storyJournal:[]};
  }
  function loadState(){try{const v=JSON.parse(localStorage.getItem(STORAGE_KEY));return v&&v.cards?migrate(v):defaultState()}catch{return defaultState()}}
  function migrate(s){s.stats=s.stats||{correct:0,wrong:0,sessions:0,streak:0,lastSession:null,history:[]};s.stats.history=s.stats.history||[];s.storyJournal=s.storyJournal||[];s.cards=s.cards.map(c=>({correct:0,wrong:0,streak:0,mastery:0,lastPlayed:null,nextReview:today(),createdAt:Date.now(),...c}));return s}
  function loadPrefs(){try{return {theme:'light',music:false,sfx:true,...JSON.parse(localStorage.getItem(PREF_KEY))}}catch{return{theme:'light',music:false,sfx:true}}}
  function save(){localStorage.setItem(STORAGE_KEY,JSON.stringify(state));renderHomeStats()}
  function savePrefs(){localStorage.setItem(PREF_KEY,JSON.stringify(prefs))}

  function init(){
    document.documentElement.dataset.theme=prefs.theme;
    createFireflies(); updateControls(); setDateLabel(); bindUI(); renderHomeStats(); renderLibrary(); renderStats();
  }
  function bindUI(){
    $$('[data-view]').forEach(b=>b.addEventListener('click',()=>showView(b.dataset.view)));
    $$('.mode-card').forEach(b=>b.addEventListener('click',()=>startGame(b.dataset.mode)));
    $('#themeBtn').onclick=toggleTheme; $('#musicBtn').onclick=toggleMusic; $('#sfxBtn').onclick=()=>{prefs.sfx=!prefs.sfx;savePrefs();updateControls();beep(440,.05)};
    $('#startDailyBtn').onclick=startDaily; $('#reviewDueBtn').onclick=()=>startGame('reverse',{dueOnly:true});
    $('#addCardBtn').onclick=()=>openCardModal(); $('#closeCardModal').onclick=closeCardModal; $('#cancelCardBtn').onclick=closeCardModal; $('#cardForm').onsubmit=saveCardForm;
    $('#searchInput').oninput=renderLibrary; $('#categoryFilter').onchange=renderLibrary; $('#sortFilter').onchange=renderLibrary;
    $('#exitGameBtn').onclick=()=>{if(currentGame&&confirm('Leave this game? Current round progress will be lost.')){currentGame=null;showView('home')}};
    $('#resetStatsBtn').onclick=resetStats;
    window.addEventListener('keydown',e=>{if(e.key==='Escape'&&!$('#cardModal').classList.contains('hidden'))closeCardModal()});
  }
  function showView(name){$$('.view').forEach(v=>v.classList.remove('active'));const el=$(`#${name}View`);if(el)el.classList.add('active');if(name==='library')renderLibrary();if(name==='stats')renderStats();window.scrollTo({top:0,behavior:'smooth'})}
  function setDateLabel(){const d=new Intl.DateTimeFormat('en-US',{weekday:'long',month:'long',day:'numeric'}).format(new Date());$('#dateLabel').textContent=`GOOD MORNING · ${d.toUpperCase()}`}
  function createFireflies(){const box=$('#fireflies');for(let i=0;i<28;i++){const f=document.createElement('i');f.className='firefly';f.style.left=Math.random()*100+'%';f.style.top=(25+Math.random()*65)+'%';f.style.setProperty('--d',(2.5+Math.random()*4)+'s');f.style.animationDelay=(-Math.random()*5)+'s';box.appendChild(f)}}
  function toggleTheme(){prefs.theme=prefs.theme==='light'?'dark':'light';document.documentElement.dataset.theme=prefs.theme;savePrefs();updateControls();beep(520,.05)}
  function updateControls(){$('#themeBtn').textContent=prefs.theme==='light'?'☀':'☾';$('#musicBtn').textContent=prefs.music?'♫':'♪';$('#sfxBtn').textContent=prefs.sfx?'🔊':'🔇'}
  function toggleMusic(){prefs.music=!prefs.music;savePrefs();updateControls();prefs.music?startMusic():stopMusic()}
  function ensureAudio(){if(!audioCtx)audioCtx=new (window.AudioContext||window.webkitAudioContext)();if(audioCtx.state==='suspended')audioCtx.resume()}
  function beep(freq=500,dur=.08,type='sine'){if(!prefs.sfx)return;ensureAudio();const o=audioCtx.createOscillator(),g=audioCtx.createGain();o.type=type;o.frequency.value=freq;g.gain.setValueAtTime(.045,audioCtx.currentTime);g.gain.exponentialRampToValueAtTime(.001,audioCtx.currentTime+dur);o.connect(g).connect(audioCtx.destination);o.start();o.stop(audioCtx.currentTime+dur)}
  function startMusic(){ensureAudio();stopMusic();const master=audioCtx.createGain();master.gain.value=.025;master.connect(audioCtx.destination);musicNodes.push(master);const base=prefs.theme==='dark'?110:196;[1,1.5,2].forEach((m,i)=>{const o=audioCtx.createOscillator(),g=audioCtx.createGain();o.type=i===0?'sine':'triangle';o.frequency.value=base*m;g.gain.value=i===0?.35:.12;o.connect(g).connect(master);o.start();musicNodes.push(o,g)});}
  function stopMusic(){musicNodes.forEach(n=>{try{if(n.stop)n.stop();if(n.disconnect)n.disconnect()}catch{}});musicNodes=[]}
  function toast(msg){const t=$('#toast');t.textContent=msg;t.classList.add('show');clearTimeout(toast.t);toast.t=setTimeout(()=>t.classList.remove('show'),1800)}

  function renderHomeStats(){const total=state.stats.correct+state.stats.wrong;$('#streakValue').textContent=state.stats.streak||0;$('#accuracyValue').textContent=total?Math.round(state.stats.correct/total*100)+'%':'0%';$('#masteredValue').textContent=state.cards.filter(c=>c.mastery>=80).length;$('#dueCount').textContent=`${getDueCards().length} cards due today`}
  function getDueCards(){const t=today();return state.cards.filter(c=>!c.nextReview||c.nextReview<=t)}
  function weightedPool(dueOnly=false){let cards=dueOnly?getDueCards():state.cards;if(cards.length<4)cards=state.cards;return [...cards].sort((a,b)=>scoreNeed(b)-scoreNeed(a));}
  function scoreNeed(c){const due=(!c.nextReview||c.nextReview<=today())?35:0;return due+(100-(c.mastery||0))+Math.min(c.wrong||0,10)*3-Math.min(c.correct||0,20)}
  function updateCard(cardId,ok){const c=state.cards.find(x=>x.id===cardId);if(!c)return;c.lastPlayed=today();if(ok){c.correct++;c.streak=(c.streak||0)+1;c.mastery=clamp((c.mastery||0)+Math.max(5,14-c.mastery*.08),0,100);const days=c.mastery>=85?7:c.mastery>=60?3:c.mastery>=35?2:1;c.nextReview=new Date(Date.now()+days*864e5).toISOString().slice(0,10);state.stats.correct++}else{c.wrong++;c.streak=0;c.mastery=clamp((c.mastery||0)-12,0,100);c.nextReview=today();state.stats.wrong++}save()}
  function finishSession(label,score,total){const d=today(),last=state.stats.lastSession;if(last!==d){const y=new Date(Date.now()-864e5).toISOString().slice(0,10);state.stats.streak=last===y?(state.stats.streak||0)+1:1;state.stats.lastSession=d}state.stats.sessions++;state.stats.history.unshift({id:uid(),date:new Date().toISOString(),label,score,total});state.stats.history=state.stats.history.slice(0,30);save();renderStats()}

  function renderLibrary(){
    const q=normalize($('#searchInput')?.value),cat=$('#categoryFilter')?.value||'all',sort=$('#sortFilter')?.value||'recent';
    const cats=[...new Set(state.cards.map(c=>c.category||'Uncategorized'))].sort();const sel=$('#categoryFilter');if(sel){const prev=sel.value;sel.innerHTML='<option value="all">All categories</option>'+cats.map(c=>`<option value="${esc(c)}">${esc(c)}</option>`).join('');sel.value=cats.includes(prev)?prev:'all'}
    let cards=state.cards.filter(c=>{const text=normalize([c.english,c.indo,c.category,c.note].join(' '));return (!q||text.includes(q))&&(cat==='all'||c.category===cat)});
    if(sort==='weak')cards.sort((a,b)=>(a.mastery||0)-(b.mastery||0));else if(sort==='mastery')cards.sort((a,b)=>(b.mastery||0)-(a.mastery||0));else if(sort==='alpha')cards.sort((a,b)=>a.english.localeCompare(b.english));else cards.sort((a,b)=>(b.createdAt||0)-(a.createdAt||0));
    const grid=$('#libraryGrid');if(!grid)return;grid.innerHTML=cards.map(c=>`<article class="memory-card"><span class="category">${esc(c.category||'Uncategorized')}</span><h3>${esc(c.english)}</h3><div class="translation">${esc(c.indo)}</div><p>${esc(c.note||'No note yet.')}</p><div class="mastery-row"><div class="mastery-track"><i style="width:${Math.round(c.mastery||0)}%"></i></div><span>${Math.round(c.mastery||0)}%</span></div><div class="card-actions"><button data-edit="${c.id}">Edit</button><button data-delete="${c.id}">Delete</button></div></article>`).join('');
    $('#libraryEmpty').classList.toggle('hidden',cards.length>0);grid.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>openCardModal(b.dataset.edit));grid.querySelectorAll('[data-delete]').forEach(b=>b.onclick=()=>deleteCard(b.dataset.delete));
  }
  function openCardModal(id){const c=state.cards.find(x=>x.id===id);$('#cardModalTitle').textContent=c?'Edit Memory':'Add Memory';$('#cardId').value=c?.id||'';$('#englishInput').value=c?.english||'';$('#indoInput').value=c?.indo||'';$('#categoryInput').value=c?.category||'Motivation';$('#noteInput').value=c?.note||'';$('#cardModal').classList.remove('hidden');setTimeout(()=>$('#englishInput').focus(),40)}
  function closeCardModal(){$('#cardModal').classList.add('hidden');$('#cardForm').reset()}
  function saveCardForm(e){e.preventDefault();const id=$('#cardId').value;const data={english:$('#englishInput').value.trim(),indo:$('#indoInput').value.trim(),category:$('#categoryInput').value.trim()||'Uncategorized',note:$('#noteInput').value.trim()};if(!data.english||!data.indo)return;if(id){Object.assign(state.cards.find(c=>c.id===id),data)}else state.cards.unshift({id:uid(),...data,correct:0,wrong:0,streak:0,mastery:0,lastPlayed:null,nextReview:today(),createdAt:Date.now()});save();closeCardModal();renderLibrary();toast(id?'Memory updated':'Memory added');beep(620,.07)}
  function deleteCard(id){const c=state.cards.find(x=>x.id===id);if(!c||!confirm(`Delete “${c.english}”?`))return;state.cards=state.cards.filter(x=>x.id!==id);save();renderLibrary();toast('Memory deleted')}

  function renderStats(){const total=state.stats.correct+state.stats.wrong;$('#statsStreak').textContent=state.stats.streak||0;$('#statsAccuracy').textContent=total?Math.round(state.stats.correct/total*100)+'%':'0%';$('#statsMastered').textContent=state.cards.filter(c=>c.mastery>=80).length;$('#statsSessions').textContent=state.stats.sessions||0;const weak=[...state.cards].sort((a,b)=>scoreNeed(b)-scoreNeed(a)).slice(0,8);$('#weakList').innerHTML=weak.map(c=>`<div class="weak-item"><div><strong>${esc(c.english)}</strong><small>${esc(c.indo)}</small></div><div class="mastery-track"><i style="width:${Math.round(c.mastery||0)}%"></i></div><b>${Math.round(c.mastery||0)}%</b></div>`).join('')||'<p>No cards yet.</p>';$('#sessionHistory').innerHTML=(state.stats.history||[]).slice(0,10).map(h=>`<div class="history-row"><div><strong>${esc(h.label)}</strong><small>${new Date(h.date).toLocaleString()}</small></div><b>${h.score}/${h.total}</b></div>`).join('')||'<p>No completed sessions yet.</p>'}
  function resetStats(){if(!confirm('Reset all mastery and progress statistics? Your cards will be kept.'))return;state.cards.forEach(c=>Object.assign(c,{correct:0,wrong:0,streak:0,mastery:0,lastPlayed:null,nextReview:today()}));state.stats={correct:0,wrong:0,sessions:0,streak:0,lastSession:null,history:[]};state.storyJournal=[];save();renderStats();toast('Progress reset')}

  function setGameHeader(title,round='Round 1',score=0){$('#gameTitle').textContent=title;$('#gameRound').textContent=round;$('#gameScore').textContent=score}
  function startGame(mode,opts={}){if(state.cards.length<6){toast('Add at least 6 cards first');return}showView('game');currentGame={mode,score:0,index:0,total:mode==='match'?6:mode==='story'?3:mode==='trail'?4:mode==='grid'?5:7,opts};if(mode==='match')setupMatch();if(mode==='recall')setupRecall();if(mode==='reverse')setupReverse();if(mode==='trail')setupTrail();if(mode==='story')setupStory();if(mode==='grid')setupGrid()}
  function nextOrComplete(renderNext,label){currentGame.index++;if(currentGame.index>=currentGame.total)return showGameComplete(label);renderNext()}
  function showGameComplete(label){finishSession(label,currentGame.score,currentGame.total);setGameHeader(label,'Complete',currentGame.score);$('#gameStage').innerHTML=`<div class="game-content session-complete"><div class="trophy">🌱</div><span class="eyebrow">MORNING MEMORY</span><h2>Training complete.</h2><p>The flock made it through this field.</p><div class="session-metrics"><div><strong>${currentGame.score}</strong><span>score</span></div><div><strong>${currentGame.total}</strong><span>rounds</span></div><div><strong>${Math.round(currentGame.score/currentGame.total*100)}%</strong><span>accuracy</span></div></div><button class="btn primary" id="completeHome">Return to Meadow</button></div>`;$('#completeHome').onclick=()=>{currentGame=null;showView('home')};beep(740,.12)}

  function setupMatch(){currentGame.total=6;currentGame.matches=0;currentGame.flips=[];currentGame.lock=false;const cards=sample(weightedPool(),6);currentGame.matchCards=shuffle(cards.flatMap(c=>[{pair:c.id,id:uid(),text:c.english,lang:'EN'},{pair:c.id,id:uid(),text:c.indo,lang:'ID'}]));setGameHeader('Classic Match','Find 6 pairs',0);renderMatch()}
  function renderMatch(){const g=currentGame;$('#gameStage').innerHTML=`<div class="game-content"><span class="eyebrow">CLASSIC MATCH</span><h2>Find the bilingual pairs.</h2><p>English and Indonesian cards belong together.</p><div class="match-grid">${g.matchCards.map(c=>`<button class="match-card ${g.flips.includes(c.id)?'flipped':''} ${c.done?'matched':''}" data-card="${c.id}" ${c.done?'disabled':''}><span class="match-face match-front">✦</span><span class="match-face match-back"><small>${c.lang}</small><br>${esc(c.text)}</span></button>`).join('')}</div></div>`;$$('[data-card]').forEach(b=>b.onclick=()=>flipMatch(b.dataset.card))}
  function flipMatch(id){const g=currentGame;if(g.lock||g.flips.includes(id))return;g.flips.push(id);beep(480,.04);renderMatch();if(g.flips.length===2){g.lock=true;const [a,b]=g.flips.map(x=>g.matchCards.find(c=>c.id===x));const ok=a.pair===b.pair&&a.id!==b.id;if(ok){setTimeout(()=>{g.matchCards.filter(c=>c.pair===a.pair).forEach(c=>c.done=true);g.matches++;g.score++;$('#gameScore').textContent=g.score;updateCard(a.pair,true);g.flips=[];g.lock=false;beep(700,.07);if(g.matches===6)showGameComplete('Classic Match');else renderMatch()},380)}else{setTimeout(()=>{updateCard(a.pair,false);updateCard(b.pair,false);g.flips=[];g.lock=false;beep(220,.08,'square');renderMatch()},700)}}}

  function setupRecall(){currentGame.items=sample(weightedPool(currentGame.opts.dueOnly),currentGame.total);setGameHeader('Word Recall','Round 1',0);renderRecall()}
  function renderRecall(){const g=currentGame,c=g.items[g.index];setGameHeader('Word Recall',`Round ${g.index+1} of ${g.total}`,g.score);$('#gameStage').innerHTML=`<div class="game-content"><span class="eyebrow">LOOK CAREFULLY</span><h2>Remember this word.</h2><div class="big-word">${esc(c.english)}</div><span class="countdown-pill">Click when you are ready</span><div style="margin-top:24px"><button class="btn primary" id="hideRecall">Hide the word</button></div></div>`;$('#hideRecall').onclick=()=>{beep(500,.04);$('#gameStage').innerHTML=`<div class="game-content"><span class="eyebrow">ACTIVE RECALL</span><h2>What was the word?</h2><p>Type the English word exactly as you remember it.</p><div class="answer-row"><input class="field" id="recallAnswer" autocomplete="off" autofocus placeholder="Type the word…"><button class="btn primary" id="checkRecall">Check</button></div><div id="recallFeedback"></div></div>`;setTimeout(()=>$('#recallAnswer').focus(),30);$('#checkRecall').onclick=checkRecall;$('#recallAnswer').onkeydown=e=>{if(e.key==='Enter')checkRecall()}}}
  function checkRecall(){const g=currentGame,c=g.items[g.index],input=$('#recallAnswer').value;const ok=normalize(input)===normalize(c.english);if(ok)g.score++;updateCard(c.id,ok);$('#gameScore').textContent=g.score;$('#recallFeedback').innerHTML=`<div class="feedback ${ok?'good':'bad'}">${ok?'✓ Correct':`Not quite. The word was “${esc(c.english)}”.`}</div><button class="btn ${ok?'primary':'ghost'}" id="nextRecall">${g.index+1===g.total?'Finish':'Next word'}</button>`;$('#checkRecall').disabled=true;$('#recallAnswer').disabled=true;beep(ok?720:210,.08,ok?'sine':'square');$('#nextRecall').onclick=()=>nextOrComplete(renderRecall,'Word Recall')}

  function setupReverse(){currentGame.items=sample(weightedPool(currentGame.opts.dueOnly),currentGame.total);setGameHeader('Reverse Recall','Round 1',0);renderReverse()}
  function renderReverse(){const g=currentGame,c=g.items[g.index],direction=Math.random()<.5?'en-id':'id-en';g.direction=direction;const prompt=direction==='en-id'?c.english:c.indo,target=direction==='en-id'?c.indo:c.english;g.target=target;setGameHeader('Reverse Recall',`Round ${g.index+1} of ${g.total}`,g.score);$('#gameStage').innerHTML=`<div class="game-content"><span class="eyebrow">${direction==='en-id'?'ENGLISH → INDONESIAN':'INDONESIAN → ENGLISH'}</span><h2>${esc(prompt)}</h2><p>Retrieve the matching word or meaning.</p><div class="answer-row"><input class="field" id="reverseAnswer" autocomplete="off" placeholder="Your answer…"><button class="btn primary" id="checkReverse">Check</button></div><div id="reverseFeedback"></div></div>`;setTimeout(()=>$('#reverseAnswer').focus(),30);$('#checkReverse').onclick=checkReverse;$('#reverseAnswer').onkeydown=e=>{if(e.key==='Enter')checkReverse()}}
  function checkReverse(){const g=currentGame,c=g.items[g.index];const ok=normalize($('#reverseAnswer').value)===normalize(g.target);if(ok)g.score++;updateCard(c.id,ok);$('#gameScore').textContent=g.score;$('#reverseFeedback').innerHTML=`<div class="feedback ${ok?'good':'bad'}">${ok?'✓ Correct':`Answer: “${esc(g.target)}”`}</div><button class="btn ${ok?'primary':'ghost'}" id="nextReverse">${g.index+1===g.total?'Finish':'Next'}</button>`;$('#checkReverse').disabled=true;$('#reverseAnswer').disabled=true;beep(ok?720:210,.08,ok?'sine':'square');$('#nextReverse').onclick=()=>nextOrComplete(renderReverse,'Reverse Recall')}

  function setupTrail(){currentGame.trailSize=3;setGameHeader('Memory Trail','Round 1',0);renderTrailStudy()}
  function renderTrailStudy(){const g=currentGame;g.trail=sample(weightedPool(),g.trailSize);g.answer=[];setGameHeader('Memory Trail',`Round ${g.index+1} of ${g.total}`,g.score);$('#gameStage').innerHTML=`<div class="game-content"><span class="eyebrow">FOLLOW THE TRAIL</span><h2>Remember the order.</h2><div class="trail-display">${g.trail.map((c,i)=>`<span class="trail-token">${i+1}. ${esc(c.english)}</span>`).join('')}</div><p>${g.trailSize} words. The trail grows as you progress.</p><button class="btn primary" id="hideTrail">I remember</button></div>`;$('#hideTrail').onclick=renderTrailAnswer}
  function renderTrailAnswer(){const g=currentGame;g.choices=shuffle(g.trail);$('#gameStage').innerHTML=`<div class="game-content"><span class="eyebrow">REBUILD THE TRAIL</span><h2>Put them back in order.</h2><div class="trail-answer" id="trailAnswer"><small>Choose the first word…</small></div><div class="trail-display">${g.choices.map(c=>`<button class="trail-choice" data-trail="${c.id}">${esc(c.english)}</button>`).join('')}</div><button class="btn primary" id="checkTrail" disabled>Check trail</button></div>`;$$('[data-trail]').forEach(b=>b.onclick=()=>{g.answer.push(b.dataset.trail);b.classList.add('used');const box=$('#trailAnswer');box.innerHTML=g.answer.map((id,i)=>`<span class="trail-token">${i+1}. ${esc(g.trail.find(c=>c.id===id).english)}</span>`).join('');$('#checkTrail').disabled=g.answer.length!==g.trail.length});$('#checkTrail').onclick=()=>{const ok=g.answer.every((id,i)=>id===g.trail[i].id);if(ok)g.score++;g.trail.forEach(c=>updateCard(c.id,ok));$('#gameScore').textContent=g.score;beep(ok?740:220,.09,ok?'sine':'square');$('#gameStage').innerHTML=`<div class="game-content"><span class="eyebrow">${ok?'TRAIL COMPLETE':'TRAIL BROKEN'}</span><h2>${ok?'Perfect order.':'Not this time.'}</h2><div class="trail-display">${g.trail.map((c,i)=>`<span class="trail-token">${i+1}. ${esc(c.english)}</span>`).join('')}</div><button class="btn primary" id="nextTrail">${g.index+1===g.total?'Finish':'Next trail'}</button></div>`;$('#nextTrail').onclick=()=>{g.index++;if(g.index>=g.total)return showGameComplete('Memory Trail');g.trailSize=Math.min(6,g.trailSize+1);renderTrailStudy()}}}

  function setupStory(){currentGame.total=3;setGameHeader('Story Chain','Round 1',0);renderStory()}
  function renderStory(){const g=currentGame;g.words=sample(weightedPool(),3);setGameHeader('Story Chain',`Round ${g.index+1} of ${g.total}`,g.score);$('#gameStage').innerHTML=`<div class="game-content story-box"><span class="eyebrow">MAKE A CONNECTION</span><h2>Use all three words.</h2><div class="story-words">${g.words.map(c=>`<span class="story-word">${esc(c.english)}</span>`).join('')}</div><p>Write one meaningful sentence. Your own connection is the memory cue.</p><textarea class="field" id="storyInput" placeholder="Write your sentence…"></textarea><div style="margin-top:14px"><button class="btn primary" id="saveStory">Save sentence</button></div></div>`;$('#saveStory').onclick=()=>{const text=$('#storyInput').value.trim();const used=g.words.filter(c=>normalize(text).includes(normalize(c.english))).length;const ok=text.length>=15&&used===3;if(!ok){toast('Use all three English words in one sentence');beep(220,.07,'square');return}g.score++;g.words.forEach(c=>updateCard(c.id,true));state.storyJournal.unshift({id:uid(),date:new Date().toISOString(),words:g.words.map(c=>c.english),text});state.storyJournal=state.storyJournal.slice(0,40);save();$('#gameScore').textContent=g.score;beep(720,.08);$('#gameStage').innerHTML=`<div class="game-content story-box"><span class="eyebrow">MEMORY CREATED</span><h2>Now retrieve the three words.</h2><p>Your sentence has been hidden. Type the three words, separated by commas.</p><div class="answer-row"><input class="field" id="storyRecall" placeholder="word, word, word"><button class="btn primary" id="checkStoryRecall">Check</button></div><div id="storyFeedback"></div></div>`;$('#checkStoryRecall').onclick=()=>{const answers=$('#storyRecall').value.split(',').map(normalize).filter(Boolean);const targets=g.words.map(c=>normalize(c.english));const count=new Set(answers.filter(a=>targets.includes(a))).size;$('#storyFeedback').innerHTML=`<div class="feedback ${count===3?'good':'bad'}">You recalled ${count}/3: ${g.words.map(c=>esc(c.english)).join(' · ')}</div><button class="btn primary" id="nextStory">${g.index+1===g.total?'Finish':'Next chain'}</button>`;$('#checkStoryRecall').disabled=true;$('#storyRecall').disabled=true;$('#nextStory').onclick=()=>nextOrComplete(renderStory,'Story Chain')}}}

  function setupGrid(){currentGame.total=5;setGameHeader('Flash Grid','Round 1',0);renderGridStudy()}
  function renderGridStudy(){const g=currentGame;g.cells=sample(weightedPool(),9);g.target=g.cells[Math.floor(Math.random()*g.cells.length)];setGameHeader('Flash Grid',`Round ${g.index+1} of ${g.total}`,g.score);$('#gameStage').innerHTML=`<div class="game-content"><span class="eyebrow">FLASH GRID</span><h2>Memorize the positions.</h2><p>You will be asked where one word was.</p><div class="flash-grid">${g.cells.map(c=>`<div class="flash-cell">${esc(c.english)}</div>`).join('')}</div><button class="btn primary" id="coverGrid">Hide grid</button></div>`;$('#coverGrid').onclick=renderGridQuestion}
  function renderGridQuestion(){const g=currentGame;$('#gameStage').innerHTML=`<div class="game-content"><span class="eyebrow">WHERE WAS IT?</span><h2>${esc(g.target.english)}</h2><p>Choose its original position.</p><div class="flash-grid">${g.cells.map((c,i)=>`<button class="flash-cell covered" data-pos="${i}" aria-label="Grid position ${i+1}">${esc(c.english)}</button>`).join('')}</div><div id="gridFeedback"></div></div>`;$$('[data-pos]').forEach(b=>b.onclick=()=>{const pos=+b.dataset.pos,targetPos=g.cells.findIndex(c=>c.id===g.target.id),ok=pos===targetPos;if(ok)g.score++;updateCard(g.target.id,ok);$('#gameScore').textContent=g.score;$$('[data-pos]').forEach((x,i)=>{x.disabled=true;x.classList.remove('covered');if(i===targetPos)x.classList.add('correct');if(i===pos&&!ok)x.classList.add('wrong')});beep(ok?740:220,.08,ok?'sine':'square');$('#gridFeedback').innerHTML=`<div class="feedback ${ok?'good':'bad'}">${ok?'✓ Correct position':'The highlighted green cell was correct.'}</div><button class="btn primary" id="nextGrid">${g.index+1===g.total?'Finish':'Next grid'}</button>`;$('#nextGrid').onclick=()=>nextOrComplete(renderGridStudy,'Flash Grid')})}

  function startDaily(){const modes=['reverse','recall','trail','grid'];let idx=0,totalScore=0,totalRounds=0;showView('game');const runNext=()=>{if(idx>=modes.length){currentGame={score:totalScore,total:totalRounds};return showGameComplete('Morning Session')}const mode=modes[idx++];startGame(mode,{dueOnly:mode==='reverse'});const original=showGameComplete;showGameComplete=function(label){totalScore+=currentGame.score;totalRounds+=currentGame.total;showGameComplete=original;currentGame=null;runNext()}};runNext()}

  init();
})();
